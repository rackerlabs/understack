# OpenStack Resource Controller Examples

Examples using [OpenStack Resource Controller (ORC)](https://k-orc.cloud/) with UnderStack.

This example creates a network, subnet, and server.

``` bash
cd examples/openstack-resource/controller/
kubectl apply --server-side -f .
```
