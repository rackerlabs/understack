---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: cluster-data
    literals:
      - UNDERSTACK_PARTITION=""
      - UNDERSTACK_SITE=""
    options:
      disableNameSuffixHash: true
