---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ironicrunbooks.ironic.understack.rackspace.net
spec:
  group: ironic.understack.rackspace.net
  names:
    kind: IronicRunbook
    listKind: IronicRunbookList
    plural: ironicrunbooks
    shortNames:
    - irb
    singular: ironicrunbook
  scope: Namespaced
  versions:
  - name: v1alpha1
    served: true
    storage: true
    additionalPrinterColumns:
    - name: Runbook
      type: string
      jsonPath: .spec.runbookName
    - name: Description
      type: string
      jsonPath: .spec.description
      priority: 1
    - name: Public
      type: boolean
      jsonPath: .spec.public
    - name: SyncStatus
      type: string
      jsonPath: .status.syncStatus
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
    schema:
      openAPIV3Schema:
        description: >-
          IronicRunbook defines one Ironic runbook. The operator-owned API
          contract keeps OpenStack credentials on every CR so reconciliation can
          be grouped by cloud, matching the other openstack-sync plugins.
        type: object
        required:
        - spec
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            description: IronicRunbookSpec defines the desired runbook data.
            type: object
            required:
            - cloudCredentialsRef
            - runbookName
            - steps
            properties:
              cloudCredentialsRef:
                description: >-
                  cloudCredentialsRef points to a Kubernetes Secret containing
                  an OpenStack clouds.yaml file. The operator reads this secret
                  directly at reconcile time; no volume mount is required.
                type: object
                required:
                - secretName
                - cloudName
                properties:
                  secretName:
                    description: >-
                      Name of a Secret in the same namespace as this resource.
                      The Secret must contain a key named clouds.yaml holding
                      an OpenStack clouds.yaml file.
                    type: string
                    minLength: 1
                    maxLength: 253
                  cloudName:
                    description: >-
                      Name of the cloud entry within the clouds.yaml to
                      authenticate as.
                    type: string
                    minLength: 1
                    maxLength: 256
              runbookName:
                description: >-
                  Runbook name, and the identity the operator syncs by. Renaming
                  creates a new runbook rather than renaming the existing one.
                type: string
                minLength: 1
                maxLength: 255
                pattern: ^[A-Za-z0-9._~-]+$
              description:
                description: Human-readable runbook description.
                type: string
                maxLength: 255
              traits:
                description: >-
                  Traits deciding which nodes this runbook may act on. A node
                  must carry at least one; a runbook with no traits matches no nodes.
                type: array
                default: []
                items:
                  type: string
                  minLength: 1
                  maxLength: 255
                  pattern: ^CUSTOM_[A-Z0-9_]+$
              steps:
                description: Ordered runbook steps.
                type: array
                minItems: 1
                items:
                  type: object
                  required:
                  - interface
                  - step
                  - order
                  properties:
                    interface:
                      description: Interface that owns this cleaning step.
                      type: string
                      enum:
                      - bios
                      - deploy
                      - firmware
                      - management
                      - power
                      - raid
                      - vendor
                    step:
                      description: Step name for the selected interface.
                      type: string
                      minLength: 1
                      maxLength: 255
                    args:
                      description: Step-specific arguments.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    order:
                      description: Execution order. Lower numbers run first.
                      type: integer
                      minimum: 0
              disableRamdisk:
                description: Whether to run without booting the cleaning ramdisk.
                type: boolean
                default: false
              public:
                description: >-
                  Whether every project can see the runbook. A public runbook
                  cannot have an owner, and only a system-scoped credential can
                  create one or set this on an existing runbook. Note that
                  public governs visibility, not use: Ironic authorises a clean
                  or service request against the runbook's owner alone, so a
                  public runbook can be used only by a system-scoped or
                  role:service token, never by a project-scoped one.
                type: boolean
                default: false
              owner:
                description: >-
                  Project that owns this runbook, and the only project whose
                  manager or admin may use it. Unset leaves the field to Ironic,
                  which fills it in for a project-scoped credential and leaves
                  it empty for a system-scoped one; a runbook that is neither
                  public nor owned can be seen and used by nobody. Removing this
                  field from a CR does not clear an owner Ironic already
                  assigned -- set public to true to clear it, which Ironic does
                  itself. Moving a runbook between public and owned needs a
                  system-scoped credential either way.
                type: string
                minLength: 1
                maxLength: 255
              extra:
                description: >-
                  Additional runbook metadata. The operator also keeps its
                  ownership markers here, under _understack_runbook_ keys.
                type: object
                x-kubernetes-preserve-unknown-fields: true
          status:
            description: IronicRunbookStatus defines the observed sync state.
            type: object
            properties:
              syncStatus:
                description: SyncStatus indicates the synchronization state with Ironic.
                type: string
                enum:
                - Synced
                - Failed
                - Unknown
              lastSyncTime:
                description: LastSyncTime is the last time the operator attempted to sync the runbook.
                type: string
                format: date-time
              observedGeneration:
                description: ObservedGeneration is the metadata generation last processed by the operator.
                type: integer
                format: int64
              message:
                description: Message provides details about the last sync attempt.
                type: string
                maxLength: 2048
              conditions:
                description: Conditions describe current observed state.
                type: array
                items:
                  type: object
                  required:
                  - type
                  - status
                  properties:
                    type:
                      type: string
                    status:
                      type: string
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                    reason:
                      type: string
                    message:
                      type: string
                      maxLength: 2048
                    lastTransitionTime:
                      type: string
                      format: date-time
    subresources:
      status: {}
