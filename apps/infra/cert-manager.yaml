---
component: cert-manager
sources:
  - repoURL: https://charts.jetstack.io
    chart: cert-manager
    targetRevision: '1.18.2'
    helm:
      releaseName: cert-manager
      valuesObject:
        crds:
          enabled: true
