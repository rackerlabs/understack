apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nautobotop.fullname" . }}-test-connection"
  labels:
    {{- include "nautobotop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nautobotop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
