apiVersion: dex.rax.io/v1alpha1
kind: Client
metadata:
  labels:
  name: bob-client
spec:
  name: bob
  secretName: bobs-secret
  generateSecret: true
  redirectURIs:
    - http://localhost:8080
    - https://some.service.example.com/
