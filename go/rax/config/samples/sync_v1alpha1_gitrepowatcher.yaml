apiVersion: sync.rax.io/v1alpha1
kind: GitRepoWatcher
metadata:
  labels:
    app.kubernetes.io/name: rax
    app.kubernetes.io/managed-by: kustomize
  name: gitrepowatcher-sample
spec:
  # TODO(user): Add fields here
