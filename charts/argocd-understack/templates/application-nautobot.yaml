{{- if eq (include "understack.isEnabled" (list $.Values.global "nautobot")) "true" }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ printf "%s-%s" $.Release.Name "nautobot" }}
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/compare-options: ServerSideDiff=true,IncludeMutationWebhook=true
spec:
  destination:
    namespace: nautobot
    server: {{ $.Values.cluster_server }}
  project: understack
  sources:
  - path: charts/nautobot-understack
    helm:
      fileParameters:
      - name: nautobot.nautobot.config
        path: files/nautobot_config.py
      ignoreMissingValueFiles: true
      releaseName: nautobot
      valueFiles:
      - $deploy/{{ include "understack.deploy_path" $ }}/nautobot/values.yaml
    repoURL: {{ include "understack.understack_url" $ }}
    targetRevision: {{ include "understack.understack_ref" $ }}
  - path: {{ include "understack.deploy_path" $ }}/nautobot
    ref: deploy
    repoURL: {{ include "understack.deploy_url" $ }}
    targetRevision: {{ include "understack.deploy_ref" $ }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    managedNamespaceMetadata:
      annotations:
        argocd.argoproj.io/sync-options: Delete=false
    syncOptions:
    - CreateNamespace=true
    - ServerSideApply=true
    - RespectIgnoreDifferences=true
    - ApplyOutOfSyncOnly=true
{{- end }}
